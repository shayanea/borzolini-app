# Fix pnpm symlink issues with Expo/React Native modules
# This hoists dependencies to make them accessible to native build systems
node-linker=hoisted

# Ensure peer dependencies are installed
auto-install-peers=true

# Fix for Expo module resolution in Android/iOS builds  
shamefully-hoist=true
